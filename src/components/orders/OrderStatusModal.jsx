export default function OrderStatusModal({order,onClose}){if(!order)return null;return <div className="modal-backdrop" onMouseDown={onClose}><div className="modal" onMouseDown={e=>e.stopPropagation()}><div className="section-head"><h2>Update {order[0]}</h2><button className="more" onClick={onClose}>×</button></div><label>Order status<select defaultValue={order[4]}><option>Pending</option><option>Confirmed</option><option>Processing</option><option>Shipped</option><option>Out for delivery</option><option>Delivered</option></select></label><label>Tracking number<input placeholder="Courier tracking ID"/></label><button className="button" onClick={onClose}>Save update</button></div></div>}
