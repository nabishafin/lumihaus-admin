import { X } from "lucide-react";
export default function OrderStatusModal({order,onClose,onSave}){if(!order)return null;return <div className="modal-backdrop" onMouseDown={onClose}><form className="modal order-modal" onSubmit={e=>{e.preventDefault();onSave?.(new FormData(e.currentTarget));}} onMouseDown={e=>e.stopPropagation()}><div className="section-head"><div><span className="page-kicker">ORDER DETAILS</span><h2>{order.id} · {order.name}</h2></div><button type="button" className="icon-action" onClick={onClose}><X size={17}/></button></div><div className="pipeline">{["Placed","Confirmed","Processing","Shipped","Delivered"].map((x,i)=><div className={x===order.status?"current":""} key={x}><i>{i+1}</i><span>{x}</span></div>)}</div><div className="form-grid"><label>Status<select name="status" defaultValue={order.status}><option>Placed</option><option>Confirmed</option><option>Processing</option><option>Shipped</option><option>Delivered</option><option>Cancelled</option></select></label><label>Courier<select name="courier"><option>Pathao</option><option>Steadfast</option><option>RedX</option></select></label><label className="full">Consignment / tracking number<input name="tracking" placeholder="e.g. PTH-8291046"/></label></div><div className="modal-actions"><button type="button" className="button secondary" onClick={()=>window.print()}>Print packing slip</button><button className="button">Update order</button></div></form></div>}
