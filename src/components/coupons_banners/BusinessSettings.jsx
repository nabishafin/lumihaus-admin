import { Save } from "lucide-react";import { useAdminUI } from "../../context/AdminUIContext";
export default function BusinessSettings(){const {notify}=useAdminUI();return <form className="settings-form" onSubmit={e=>{e.preventDefault();notify("bKash and delivery settings saved")}}><div className="form-grid"><label>Display bKash number<input defaultValue="01712-345678"/></label><label>Account type<select defaultValue="Personal Send Money"><option>Personal Send Money</option><option>Merchant</option></select></label><label>Inside Dhaka fee (BDT)<input type="number" defaultValue="60"/></label><label>Outside Dhaka fee (BDT)<input type="number" defaultValue="120"/></label></div><button className="button"><Save size={14}/> Save business settings</button></form>}
